-- Initialize TimescaleDB extension for time-series data
-- This is a PostgreSQL extension and may show linting warnings in standard SQL validators
CREATE EXTENSION IF NOT EXISTS timescaledb CASCADE;

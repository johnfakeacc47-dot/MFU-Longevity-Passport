# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example
node_modules/
dist/

# AI agent / tooling artifacts
.agents/
skills-lock.json
.claude/settings.local.json

# ML training assets - deliberately untracked.
# These once bloated the repo history to 173MB and were stripped out of it.
# Keep local copies outside the repo; ask the team for the dataset.
# NOTE: public/model/ is intentionally NOT ignored - the browser fetches it at runtime.
dataset/
thai_food_model.keras
thai_food_model_savedmodel/
public/model_backup/

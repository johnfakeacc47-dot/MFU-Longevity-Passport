# Dev Quick Login — enabled ONLY in local development
# This file is NOT committed to production (add to .gitignore if needed)

# Enable the Dev Quick Login button on the login page
VITE_ENABLE_DEV_LOGIN=true

# 4-digit PIN to access dev mode (change this to your preference)
VITE_DEV_LOGIN_PIN=3333
